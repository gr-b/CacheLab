//#include <stdio.h>
/*
void print_matrix(int matrix[M][N], int M, int N){
    printf("sdfsdfsfefsefsefse\n");
}

void transpose_submit(int M, int N, int A[N][M], int B[M][N])
{
    // May not modify array A. Only modify array B.
    int row, column, tmp;
    print_matrix(A,M,N);
    for (row = 0; row < N; row++) {
        for (column = 0; column < M; column++) {
            tmp = A[row][column];
            B[column][row] = tmp;
        }
    } 	
}*/

int main(){
    /*int M = 32;
    int N = 32;
    int A[N][M];
    int B[M][N];
    transpose_submit(M, N, A, B);*/
}

